<%
  parts = []
  parts << "Administration"
  parts << link_to("Back To Admin", admin_path) unless params[:controller].split('/').include?("admin")
  parts << link_to("Logout", admin_logout_path) if logged_in?
%>
<div id="admin-bar">
  <%=raw parts.join(" | ") %>
</div>